const DEFAULT_PORT = 5000;

const AppRoute = {
  MAIN: '/',
  OFFERS: '/offers',
  POSTS: '/posts',
  LANG: '/lang',
};

const StatusCode = {
  SUCCESS: 200,
}

module.exports = {DEFAULT_PORT, AppRoute, StatusCode};
